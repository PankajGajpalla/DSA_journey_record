# Intuition
* extract and do the combination simple:)

# Code
```python3 []
class Solution:
    def solve(self, digits, mapping, ans, output, index):
        if(index >= len(digits)):
            ans.append(output)
            return
        
        number = int(digits[index])
        value = mapping[number]

        for ch in value:
            # output += ch no need to make a permanent change and then remove it , instead just add it while passing and it solves the problem
            self.solve(digits, mapping, ans, output+ch, index+1)
            # output = output[:-1] 

    def letterCombinations(self, digits: str) -> List[str]:
        if digits=="": return []
        mapping = ["", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"]
        ans = []
        # output = ""
        # index = 0
        self.solve(digits, mapping, ans, "", 0)
        return ans
```
