class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        maxi = nums[0]
        tsum = 0
        for i in nums:
            tsum += i
            maxi = max(tsum, maxi)
            if(tsum<0):
                tsum = 0
        return maxi
